# Smoke test — docker-compose + Postgres + pytest

## Pré-requisitos
- Docker + Docker Compose
- Ambiente Python (Conda) criado (`make env-create`) e deps instaladas (`make deps-install-dev`)

## Configuração
1. Copie o arquivo de exemplo:
   ```bash
   cp .env.example .env